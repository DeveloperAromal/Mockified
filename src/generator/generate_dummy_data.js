import { SchemaClassifier } from "./schema/schemaClassifer";
import { generateUUID } from "../utils/uuid_generator";

export const generateData = (data) => {
    classified_data = SchemaClassifier(data)


}